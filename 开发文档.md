# AI Adaptation Studio 开发文档

## 1. 项目定位

本项目是一个“小说转剧本工作台”原型，目标是把多章节小说文本整理成可编辑的场景化剧本草稿，而不是一次性输出不可维护的大段文本。

当前版本强调三件事：

1. 多章节小说输入
2. 结构化剧本输出
3. 可继续编辑、重写和导出

## 2. 当前实现状态

当前代码已经完成：

- 文本文件上传
- 项目列表、归档和删除
- 章节切分
- 简要摘要
- 基础人物提取
- 场景拆分与剧本初稿生成
- 剧本质量体检报告
- 单场景编辑
- 单场景指令式重写
- OpenAI LLM 初稿生成与单场景重写，可回退规则逻辑
- 版本记录
- 版本对比
- YAML / JSON 导出
- 后端脚本结构校验
- 后端主流程测试

当前仍是原型的部分：

- LLM 生成和重写仍缺少生产级提示词编排、质量评估与成本控制
- 人物抽取和场景规划仍是启发式
- 没有鉴权
- 没有多用户隔离

## 3. 系统结构

### 3.1 前端

技术栈：

- `Vue 3`
- `Vite`
- `Pinia`
- `Axios`

当前页面：

- 导入页：创建项目并上传源文本
- 项目页：查看项目列表、切换项目、归档和删除项目
- 工作台：查看场景、编辑场景、重写场景、导出剧本
- 版本页：查看版本列表、切换历史版本、执行场景级版本对比

### 3.2 后端

技术栈：

- `FastAPI`
- `Pydantic v2`
- `PyYAML`

当前职责：

- 接收文件与业务请求
- 管理项目和任务状态
- 解析章节
- 生成剧本草稿
- 执行场景重写
- 配置 OpenAI 时调用真实 LLM，未配置或模型返回无效时回退规则逻辑
- 校验脚本结构
- 导出 YAML / JSON

服务层已按职责拆分：

- `common.py`：状态常量、响应封装、项目/任务基础操作、上传文件处理
- `text_analysis.py`：文本归一化、章节切分、摘要、人物和关键词提取
- `script_builder.py`：章节记录、场景拆分、剧本初稿和 LLM 初稿生成
- `scene_rewriter.py`：规则重写和 LLM 场景重写
- `script_ops.py`：脚本校验、场景查找、版本名、导出序列化
- `tasks.py`：解析、生成、编辑、重写、导出等任务编排
- `pipeline.py`：兼容导出层，保留既有导入路径

### 3.3 存储

当前使用 SQLite 持久化：

- `backend/data/studio.sqlite3`

启动时如果数据库为空，后端会尝试从旧的 `backend/data/store.json` 导入已有项目和任务数据。导入只会在空数据库上执行一次。

相关目录：

- `backend/data/uploads/`
- `backend/data/exports/`

## 4. 核心处理流程

### Step 1: 上传源文本

支持：

- `.txt`
- `.md`

### Step 2: 章节切分

当前支持的章节标题识别包括：

- `第十二章`
- `第十二节`
- `第十二回`
- `序章`
- `楔子`
- `Chapter 3`
- Markdown 标题
- `12. 标题`

如果无法按标题切分，会退化为按段落块切分。

### Step 3: 章节摘要与人物提取

当前逻辑：

- 摘要：按句切分后取前部关键信息
- 人物提取：按中文名字模式和上下文提示词做启发式提取

### Step 4: 场景拆分

当前会先把章节拆成段落组，再把每组映射成一个场景。一个章节不再固定只生成一个场景。

### Step 5: 剧本生成

生成任务会先构造一份规则草稿作为 schema 参考和兜底结果。配置 `LLM_PROVIDER=openai` 与 `OPENAI_API_KEY` 后，后端会把章节摘要、人物、章节到场景映射和草稿结构发送给 LLM 生成初稿；模型返回必须通过 Pydantic 校验，否则自动使用规则草稿。

当前输出结构包括：

- `project`
- `source_summary`
- `chapters`
- `scenes`
- `metadata`
- `versions`
- `quality_report`

场景内包括：

- `scene_id`
- `title`
- `slugline`
- `purpose`
- `source_refs`
- `characters`
- `dramatic_structure`
- `beats`
- `adaptation_notes`

### Step 6: 结构校验

后端当前已对以下内容做 Pydantic 校验：

- beat 结构
- scene 结构
- chapter 结构
- source summary 结构
- script metadata
- 全量 script 结构
- 剧本质量报告结构

生成、编辑、重写、导出都会走统一校验。

### Step 7: 剧本质量体检

生成、编辑和重写后，后端会刷新 `quality_report`。当前报告包括：

- 综合评分
- 比赛展示亮点
- 戏剧冲突、角色动机、可拍性、对白表现、节奏与钩子、原文覆盖等分项评分
- 场景级优势、风险和修改建议
- 下一轮修订优先级

这部分用于证明系统不仅生成结构化文本，还能对剧本质量做审稿和改写指导。

### Step 8: 场景重写

当前支持按自然语言指令对单场景执行局部重写。

配置 LLM 后会优先调用模型重写 `purpose`、`dramatic_structure`、`beats`、`adaptation_notes`。未配置或模型返回无效时，规则回退逻辑会尝试识别这些方向：

- 节奏压缩
- 冲突强化
- 情绪放大
- 反转前置

## 5. 版本策略

版本记录保存在项目级别：

- 初稿生成会创建首个版本
- 场景重写可创建新版本
- 手工编辑会记录当前版本的改动场景
- 版本对比会按场景标出新增、删除、修改和未变，并列出变化字段

历史版本可以在前端查看，工作台查看历史版本时会自动进入只读模式。

## 6. 测试

当前已补的后端测试覆盖：

- 标题识别
- 章节切分
- 人物提取
- 剧本生成
- 剧本质量报告
- LLM 初稿生成成功路径与回退路径
- 场景重写
- 完整 API 主流程
- 版本创建
- 导出
- 结构化错误返回

运行方式：

```bash
cd backend
pytest -q
```

## 7. 下一步建议

优先级最高的后续工作：

1. 补前端测试和更多异常场景测试
2. 增加鉴权和多用户隔离
3. 继续增强全局剧情融合与质量评估
4. 优化 LLM 提示词、成本控制和失败可观测性
5. 增加更细的版本 diff 展示和恢复能力
