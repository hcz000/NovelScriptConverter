# AI Adaptation Studio 接口文档

## 1. 基本说明

- 接口前缀：`/api/v1`
- 协议风格：`REST API`
- 任务模型：长耗时操作通过任务接口轮询
- 编码：`UTF-8`

## 2. 通用响应结构

成功响应：

```json
{
  "code": 0,
  "message": "ok",
  "request_id": "req_xxx",
  "data": {}
}
```

错误响应：

```json
{
  "code": 40002,
  "message": "unsupported file type",
  "request_id": "req_xxx",
  "data": null
}
```

## 3. 任务状态

- `PENDING`
- `RUNNING`
- `SUCCEEDED`
- `FAILED`

## 4. 主要接口

### 4.1 健康检查

```http
GET /api/v1/health
```

### 4.2 创建项目

```http
POST /api/v1/projects
Content-Type: application/json
```

请求体：

```json
{
  "title": "测试项目",
  "language": "zh-CN"
}
```

### 4.3 获取项目列表

```http
GET /api/v1/projects
```

可选查询参数：

- `include_archived`

### 4.4 获取项目详情

```http
GET /api/v1/projects/{project_id}
```

### 4.5 归档项目

```http
POST /api/v1/projects/{project_id}/archive
```

归档项目默认不会出现在项目列表中，使用 `include_archived=true` 可查看。

### 4.6 删除项目

```http
DELETE /api/v1/projects/{project_id}
```

删除项目会同时删除该项目关联任务记录。

### 4.7 上传源文本

```http
POST /api/v1/projects/{project_id}/source
Content-Type: multipart/form-data
```

支持文件：

- `.txt`
- `.md`

### 4.8 启动章节解析

```http
POST /api/v1/projects/{project_id}/parse
Content-Type: application/json
```

请求体：

```json
{
  "min_chapter_count": 3,
  "split_mode": "auto"
}
```

### 4.9 获取章节列表

```http
GET /api/v1/projects/{project_id}/chapters
```

### 4.10 启动剧本生成

```http
POST /api/v1/projects/{project_id}/generate
Content-Type: application/json
```

请求体：

```json
{
  "target_format": "yaml",
  "scene_granularity": "standard",
  "include_report": true
}
```

### 4.11 获取剧本

```http
GET /api/v1/projects/{project_id}/script
```

可选查询参数：

- `version_id`

### 4.12 获取场景列表

```http
GET /api/v1/projects/{project_id}/scenes
```

### 4.13 获取单个场景

```http
GET /api/v1/projects/{project_id}/scenes/{scene_id}
```

### 4.14 更新场景

```http
PATCH /api/v1/projects/{project_id}/scenes/{scene_id}
Content-Type: application/json
```

可更新字段：

- `title`
- `slugline`
- `purpose`
- `beats`
- `adaptation_notes`
- `change_note`

### 4.15 重写场景

```http
POST /api/v1/projects/{project_id}/scenes/{scene_id}/rewrite
Content-Type: application/json
```

请求体：

```json
{
  "instruction": "增强冲突张力，压缩节奏",
  "preserve_core_event": true,
  "create_new_version": true
}
```

### 4.16 获取版本列表

```http
GET /api/v1/projects/{project_id}/versions
```

### 4.17 对比版本

```http
GET /api/v1/projects/{project_id}/versions/compare
```

查询参数：

- `base_version_id`
- `target_version_id`

返回场景级变化：`added`、`removed`、`changed`、`unchanged`，并列出修改字段。

### 4.18 获取单个版本信息

```http
GET /api/v1/projects/{project_id}/versions/{version_id}
```

### 4.19 导出剧本

```http
POST /api/v1/projects/{project_id}/export
Content-Type: application/json
```

请求体：

```json
{
  "version_id": "ver_xxx",
  "format": "yaml",
  "include_report": true
}
```

### 4.20 查询任务

```http
GET /api/v1/tasks/{task_id}
```

### 4.21 下载导出文件

```http
GET /api/v1/downloads/{file_name}
```

当前下载接口已限制只能访问导出目录内文件。

## 5. 当前实现说明

当前接口与实现保持一致的要点：

- 剧本生成和重写会在配置 OpenAI 时优先调用 LLM，未配置或模型返回无效时回退规则原型
- 项目和任务数据持久化到 SQLite：`backend/data/studio.sqlite3`
- 后端会对剧本结构做统一 schema 校验
- 历史版本可以通过 `version_id` 查看和导出
- 版本对比按场景输出新增、删除、修改和未变状态
- 任务结果需要前端轮询 `/tasks/{task_id}`

## 6. 典型前端流程

前端当前主流程是：

1. `POST /projects`
2. `POST /projects/{project_id}/source`
3. `POST /projects/{project_id}/parse`
4. 轮询 `GET /tasks/{task_id}`
5. `POST /projects/{project_id}/generate`
6. 再次轮询 `GET /tasks/{task_id}`
7. 拉取项目、章节、剧本、版本数据
